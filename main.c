#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include "Libraries/Map/map.h"

int *t;
long idx = 0;
// 1
// [ ]
// 2 [ [] ]
void test(int dimensions) {
    t = (int *)malloc(sizeof(int) * dimensions); // Basic Array;
    memset(t, '\0', sizeof(char *) * dimensions);

    for(int i = 0; i < idx; i++) {
        t
    }
}

int main() {


    return 0;
}